import Foundation

enum Constants {
    // TODO: Replace with your Dynamic environment ID from https://app.dynamic.xyz
    static let dynamicEnvironmentId = "YOUR_DYNAMIC_ENV_ID"
    static let appLogoUrl = "https://avatars.githubusercontent.com/u/105617514"
    static let appName = "LiveStream"
    static let redirectScheme = "livestream"
    static let redirectUrl = "livestream://callback"
    static let appOrigin = "https://livestream.app"

    // Backend (stubbed for now — set to your ngrok/backend URL when ready)
    static let backendBaseUrl = "http://localhost:3000"
}
